// 注: 書籍のシンタックス説明用サンプル。プレースホルダ（式・値1・値2）を
//     実際に動く値に差し替えてコンパイルを通るようにしています。
const 式 = 1;
const 値1 = 1;
const 値2 = 2;
switch (式) {
  case 値1:
    // 値1に一致する場合の処理
    break;
  case 値2:
    // 値2に一致する場合の処理
    break;
  default:
  // どのcaseにも一致しない場合の処理
}
