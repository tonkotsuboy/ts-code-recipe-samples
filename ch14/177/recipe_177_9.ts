// 三校PDFでは「recipe_177_8.ts」と記載されていますが、
// 前掲の採番ズレに合わせて、本配布コードでは「recipe_177_9.ts」としています。
// After: フィールド宣言と代入を分けた書き方
class MyClass {
  private myField: string;
  constructor(myField: string) {
    this.myField = myField;
  }
}
